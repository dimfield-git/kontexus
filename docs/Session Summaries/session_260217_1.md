# Session Summary — 2026-02-17

## Test Suite — Complete
- Created `conftest.py` with `tmp_db` fixture — all tests run against a temporary database, real `data/contexts.db` is never touched
- `test_database.py` — 8 tests covering schema creation, migration idempotency, tier CHECK constraint, commit/rollback behavior, row factory
- `test_core.py` — 27 tests covering all eight business logic functions (add, list, get, rate, search, export, delete, merge) with edge cases
- `test_cli.py` — 20 tests using Typer's CliRunner, covering all CLI commands end-to-end
- **54 tests total, all passing**

## Bug Fix — ORDER BY Tiebreaker
- `list_contexts` and `search_contexts` in `core.py` both used `ORDER BY created DESC`, which produced non-deterministic results when entries shared the same timestamp
- Fixed by adding `id DESC` as tiebreaker: `ORDER BY created DESC, id DESC` (3 instances)

## Planned Feature — File-Based Add (Parked)
- Add `--prompt-file` (`-pf`) and `--summary-file` (`-sf`) flags to the `add` command
- Reads content from file instead of positional argument, bypassing shell character limits
- Plan documented, not yet implemented

## Phase 1 — Complete
All deliverables from the project plan are done:
- Data model + SQLite schema with migrations
- All eight CLI commands (add, list, view, rate, search, export, delete, merge)
- Tier validation (S/A/B/F)
- Error handling
- Test suite (54/54 passing)
- README with installation and usage docs
- Field-tested across GPT, Gemini, Copilot, Claude

## Next Up
- `--prompt-file` / `--summary-file` flags (when needed)
- Phase 2: FastAPI backend in `api.py`
